<Window x:Class="MDExport.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About MDExport"
        Width="460"
        Height="340"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Icon="/Assets/icon.png">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Image Source="/Assets/icon.png"
                   Width="48" Height="48"
                   Margin="0,0,14,0"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   VerticalAlignment="Center" />
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="MDExport"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextBrush}" />
                <TextBlock x:Name="VersionText"
                           FontSize="12"
                           Foreground="{StaticResource TextMutedBrush}" />
            </StackPanel>
        </StackPanel>

        <TextBlock Grid.Row="1"
                   Text="A minimalist Markdown editor with HTML, PDF and DOCX export."
                   TextWrapping="Wrap"
                   Margin="0,0,0,4"
                   Foreground="{StaticResource TextBrush}" />

        <TextBlock Grid.Row="2"
                   Text="© 2026"
                   FontSize="11"
                   Margin="0,0,0,12"
                   Foreground="{StaticResource TextMutedBrush}" />

        <Border Grid.Row="3"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                Padding="10"
                Margin="0,0,0,12">
            <TextBlock x:Name="StatusText"
                       Text="Click 'Check for Updates' to see if a new version is available."
                       TextWrapping="Wrap"
                       VerticalAlignment="Top"
                       Foreground="{StaticResource TextMutedBrush}" />
        </Border>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="CheckUpdatesButton"
                    Content="Check for Updates"
                    Style="{StaticResource AccentButton}"
                    Padding="14,6"
                    Margin="0,0,8,0"
                    Click="CheckUpdates_Click" />
            <Button Content="Close"
                    Style="{StaticResource FlatButton}"
                    Padding="14,6"
                    IsCancel="True"
                    Click="Close_Click" />
        </StackPanel>
    </Grid>
</Window>
